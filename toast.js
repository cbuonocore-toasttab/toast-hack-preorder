'use strict';
// Library containing IBM Watson utility functions for MorningDose App.
var library = (function () {

    var baseUrl = "pos.toasttab.com";

    // TODO: Functions for requesting and aggregating pre-order data by date.

    return {
        BASE_URL: baseUrl
    };
})();
module.exports = library;